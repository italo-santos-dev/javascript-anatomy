class Retangulo {
    constructor(altura, largura) {
        this.altura = altura;
        this.largura = largura;
    }
}

let Triangulo = class {
    constructor(altura, lado) {
        this.altura = altura;
        this.lado = lado
    }
}